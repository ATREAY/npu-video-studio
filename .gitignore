# env
.venv/
__pycache__/
*.pyc

# model artifacts / large files
models/*.onnx
models/*.tflite
models/*.bin
models/*.so
models/*.dlc
*.pt
*.pth
build/
.qai_hub_models/

# job outputs
aihub/jobs/*.json
benchmarks/raw/

# packaging: wheels are regenerated by build_wheelhouse.sh, not tracked (110MB of
# binaries doesn't belong in git history) — ship them as a GitHub Release asset instead
packaging/wheelhouse/*.whl

# misc
.DS_Store
*.log
